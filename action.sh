#git page action is used to generate /docs as artifacs
#no need to keep /docs in the repo
#action setup is here:
#deply from action, rather than main/docs

mkdir -p .github/workflows
touch .github/workflows/pages.yml
